# ===========================================
# PRODUCTION ENVIRONMENT VARIABLES
# ===========================================
# Copy these to Dokploy Environment Variables
# DO NOT commit actual secrets to Git!

# Node Environment
NODE_ENV=production

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# If using Dokploy PostgreSQL:
# Format: postgresql://USERNAME:PASSWORD@SERVICE_NAME:5432/DATABASE_NAME
DATABASE_URL=postgresql://medusa:YOUR_POSTGRES_PASSWORD@medusa-postgres:5432/medusa-v2

# ===========================================
# REDIS CONFIGURATION
# ===========================================
# If using Dokploy Redis:
# Format: redis://SERVICE_NAME:6379
REDIS_URL=redis://medusa-redis:6379

# ===========================================
# SECURITY SECRETS
# ===========================================
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
JWT_SECRET=REPLACE_WITH_GENERATED_SECRET_64_CHARS
COOKIE_SECRET=REPLACE_WITH_GENERATED_SECRET_64_CHARS

# ===========================================
# CORS CONFIGURATION
# ===========================================
# Comma-separated list of allowed origins
# Update with your actual domain names
STORE_CORS=https://your-store.com,https://www.your-store.com
ADMIN_CORS=https://admin.your-domain.com,https://your-domain.com/app
AUTH_CORS=https://admin.your-domain.com,https://your-domain.com/app

# ===========================================
# RAZORPAY CONFIGURATION (PRODUCTION)
# ===========================================
# Get from: https://dashboard.razorpay.com/app/keys
# Use LIVE keys (rzp_live_*), NOT test keys
RAZORPAY_KEY_ID=rzp_live_xxxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your_razorpay_live_secret_key

# Razorpay Webhook Secret
# 1. Go to: https://dashboard.razorpay.com/app/webhooks
# 2. Create webhook with URL: https://your-domain.com/webhooks/razorpay
# 3. Copy the secret
RAZORPAY_WEBHOOK_SECRET=whsec_your_webhook_secret

# ===========================================
# OPTIONAL CONFIGURATION
# ===========================================

# Backend URL (for admin panel)
MEDUSA_BACKEND_URL=https://your-domain.com

# Disable admin panel (set to "true" to disable)
# DISABLE_MEDUSA_ADMIN=false

# Worker mode (shared/worker/server)
# MEDUSA_WORKER_MODE=shared
